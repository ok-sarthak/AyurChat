# Copy this file to .env and update the values

# Server Configuration
PORT=5000

# Database Configuration
# For local MongoDB installation:
MONGODB_URI=mongodb://localhost:27017/chatbot

# For MongoDB Atlas (replace with your connection string):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/chatbot?retryWrites=true&w=majority

# JWT Secret (replace with a strong secret key)
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production

# Google Gemini AI API Key
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=your_gemini_api_key_here

# Instructions:
# 1. Copy this file to .env in the backend directory
# 2. Replace JWT_SECRET with a strong, random string
# 3. Get a Gemini API key from Google AI Studio
# 4. If using MongoDB Atlas, replace MONGODB_URI with your connection string
# 5. Make sure MongoDB is running if using local installation
